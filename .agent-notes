# Agent Notes

[state] `any-cut` has a scaffold implementation on a divergent jj head titled `loop: Scaffold the Phase 1 any-mcp workspace crate and minimal binary`; it is not integrated into the current root and must be reconciled before dependent work starts.
