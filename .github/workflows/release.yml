# This file was autogenerated by dist: https://axodotdev.github.io/cargo-dist
#
# Copyright 2022-2024, axodotdev
# SPDX-License-Identifier: MIT or Apache-2.0
#
# Cargo-dist qualification and release-input generation. This generated file
# has an allow-dirty CI edit: Linux and macOS local archives come from flake
# outputs, and tag runs export the macOS Nix binary for offline signing. Manual,
# weekly, and tag runs build and validate without publishing. A maintainer signs
# and notarizes the macOS input locally, then finalize-release.yml regenerates
# checksums and installers before publishing the draft GitHub Release.

name: Release artifacts
permissions:
  contents: read

on:
  push:
    tags:
      - "[0-9]*.[0-9]*.[0-9]*"
      - "anyr-v[0-9]*.[0-9]*.[0-9]*"
  schedule:
    - cron: "17 13 * * 0"
  workflow_dispatch:
    inputs:
      source_ref:
        description: Branch or tag to build
        required: true
        default: main
        type: string
      architecture:
        description: Architecture to build
        required: true
        default: all
        type: choice
        options:
          - all
          - aarch64-apple-darwin
          - aarch64-unknown-linux-gnu
          - aarch64-pc-windows-msvc
          - x86_64-unknown-linux-gnu
          - x86_64-pc-windows-msvc

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    runs-on: "ubuntu-22.04"
    permissions:
      contents: read
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ github.event_name == 'push' && github.ref_name || '' }}
      tag-flag: ${{ github.event_name == 'push' && format('--tag={0}', github.ref_name) || '' }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      SELECTED_ARCHITECTURE: ${{ inputs.architecture || 'all' }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.source_ref || github.ref }}
      - name: Validate release tag and main ancestry
        if: github.event_name == 'push'
        shell: bash
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          bash .github/scripts/validate-release-tag.sh "$RELEASE_TAG"
          git fetch --no-tags origin main:refs/remotes/origin/main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" refs/remotes/origin/main; then
            printf 'release tag %s does not point to a commit reachable from main\n' "$RELEASE_TAG" >&2
            exit 1
          fi
      # A tag produces signing inputs only from a commit on which every verification
      # workflow (smoke, CI, any-mcp, anytype-api live, anyr-anyback-live)
      # has already succeeded; push the tag after those main runs are green
      # or after release-qual.yml reports the commit eligible.
      - name: Require green verification workflows on the tagged commit
        if: github.event_name == 'push'
        shell: bash
        run: python3 .github/scripts/release_eligibility.py --repo "$GITHUB_REPOSITORY" --sha "$GITHUB_SHA"
      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@ef8a148080ab6020fd15196c2084a2eea5ff2d25 # v22
      - name: Verify pinned CI prerequisites
        run: nix develop --command bash .github/scripts/verify-ci-prerequisites.sh --check-repository
      - name: Install dist
        # we specify bash to get pipefail; it guards against the `curl` command
        # failing. otherwise `sh` won't catch that `curl` returned non-0
        shell: bash
        run: "curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v0.30.3/cargo-dist-installer.sh | sh"
      - name: Cache dist
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/dist
      # sure would be cool if github gave us proper conditionals...
      # so here's a doubly-nested ternary-via-truthiness to try to provide the best possible
      # functionality based on whether this is a pull_request, and whether it's from a fork.
      # (PRs run on the *source* but secrets are usually on the *target* -- that's *good*
      # but also really annoying to build CI around when it needs secrets to work right.)
      - id: plan
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          if [[ "${{ github.event_name }}" == push ]]; then
            dist host --steps=create --tag="$RELEASE_TAG" --output-format=json > plan-dist-manifest.json
          else
            dist plan --output-format=json > plan-dist-manifest.json
          fi
          if [[ "$SELECTED_ARCHITECTURE" != "all" ]]; then
            jq --arg target "$SELECTED_ARCHITECTURE" \
              '.ci.github.artifacts_matrix.include |= map(select(.targets | index($target)))' \
              plan-dist-manifest.json > selected-plan-dist-manifest.json
            test "$(jq '.ci.github.artifacts_matrix.include | length' selected-plan-dist-manifest.json)" -gt 0
            mv selected-plan-dist-manifest.json plan-dist-manifest.json
          fi
          echo "dist ran successfully"
          cat plan-dist-manifest.json
          echo "manifest=$(jq -c "." plan-dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json

  # Build and packages all the platform-specific things
  build-local-artifacts:
    name: build-local-artifacts (${{ join(matrix.targets, ', ') }})
    # Let the initial task tell us to not run (currently very blunt)
    needs:
      - plan
    if: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix.include != null }}
    strategy:
      fail-fast: false
      # Target platforms/runners are computed by dist in create-release.
      # Each member of the matrix has the following arguments:
      #
      # - runner: the github runner
      # - dist-args: cli flags to pass to dist
      # - install-dist: expression to run to install dist on the runner
      #
      # Typically there will be:
      # - 1 "global" task that builds universal installers
      # - N "local" tasks that build each platform's binaries and platform-specific installers
      matrix: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container && matrix.container.image || null }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}-dist-manifest.json
    steps:
      - name: enable windows longpaths
        run: |
          git config --global core.longpaths true
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.source_ref || github.ref }}
      - name: Install Rust non-interactively if not already installed
        if: ${{ matrix.container }}
        run: |
          if ! command -v cargo > /dev/null 2>&1; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          fi
      - name: Install dist
        run: ${{ matrix.install_dist.run }}
      - name: Install Nix
        if: runner.os != 'Windows'
        uses: DeterminateSystems/nix-installer-action@ef8a148080ab6020fd15196c2084a2eea5ff2d25 # v22
      # Get the dist-manifest
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - name: Install dependencies
        if: runner.os == 'Windows'
        run: |
          ${{ matrix.packages_install }}
      - name: Build Windows artifacts
        if: runner.os == 'Windows'
        run: |
          # Actually do builds and make zips and whatnot
          dist build ${{ needs.plan.outputs.tag-flag }} --print=linkage --output-format=json ${{ matrix.dist_args }} > dist-manifest.json
          echo "dist ran successfully"
      - name: Build Nix release binary
        if: runner.os != 'Windows'
        shell: bash
        env:
          RELEASE_TAG: ${{ needs.plan.outputs.tag }}
          RELEASE_TARGET: ${{ matrix.targets[0] }}
        run: |
          set -euo pipefail
          case "$RELEASE_TARGET" in
            aarch64-apple-darwin)
              nix_attr=.#anytype-toolbox-aarch64-darwin
              ;;
            aarch64-unknown-linux-gnu | x86_64-unknown-linux-gnu)
              nix_attr=.#anytype-toolbox-static
              ;;
            *)
              printf 'unsupported Nix release target: %s\n' "$RELEASE_TARGET" >&2
              exit 2
              ;;
          esac
          nix build -L "$nix_attr"
          bash .github/scripts/package-nix-dist-archive.sh "$RELEASE_TARGET" result/bin/anyr
      - name: Verify Nix release binary portability
        if: runner.os != 'Windows'
        shell: bash
        env:
          RELEASE_TARGET: ${{ matrix.targets[0] }}
        run: |
          set -euo pipefail
          if [[ "$RELEASE_TARGET" == aarch64-apple-darwin ]]; then
            otool -L result/bin/anyr
            if otool -L result/bin/anyr | grep -q '/nix/store'; then
              printf 'anyr references /nix/store dylibs and is not portable\n' >&2
              exit 1
            fi
          else
            file result/bin/anyr
            file result/bin/anyr | grep -Eq 'statically linked|static-pie linked' || {
              printf 'anyr is not statically linked\n' >&2
              exit 1
            }
          fi
          result/bin/anyr --help > /dev/null
      - name: Prepare macOS offline-signing input
        if: github.event_name == 'push' && matrix.targets[0] == 'aarch64-apple-darwin'
        shell: bash
        env:
          RELEASE_TAG: ${{ needs.plan.outputs.tag }}
        run: |
          set -euo pipefail
          signing_dir=target/macos-signing-input
          mkdir -p "$signing_dir"
          cp -L result/bin/anyr "$signing_dir/anyr"
          chmod 0755 "$signing_dir/anyr"
          binary_hash=$(shasum -a 256 "$signing_dir/anyr" | awk '{print $1}')
          flake_lock_hash=$(shasum -a 256 flake.lock | awk '{print $1}')
          jq -n \
            --arg repository "$GITHUB_REPOSITORY" \
            --argjson source_run_id "$GITHUB_RUN_ID" \
            --arg source_commit "$GITHUB_SHA" \
            --arg release_tag "$RELEASE_TAG" \
            --arg binary_sha256 "$binary_hash" \
            --arg flake_lock_sha256 "$flake_lock_hash" \
            '{
              schema_version: 1,
              repository: $repository,
              source_run_id: $source_run_id,
              source_commit: $source_commit,
              release_tag: $release_tag,
              target: "aarch64-apple-darwin",
              binary_sha256: $binary_sha256,
              flake_lock_sha256: $flake_lock_sha256
            }' > "$signing_dir/manifest.json"
      - id: cargo-dist
        name: Post-build
        # We force bash here just because github makes it really hard to get values up
        # to "real" actions without writing to env-vars, and writing to env-vars has
        # inconsistent syntax between shell and powershell.
        shell: bash
        run: |
          # Parse out what we just built and upload it to scratch storage
          {
            echo "paths<<EOF"
            dist print-upload-files-from-manifest --manifest dist-manifest.json
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}
      - name: Upload macOS offline-signing input
        if: github.event_name == 'push' && matrix.targets[0] == 'aarch64-apple-darwin'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: macos-signing-input
          path: target/macos-signing-input/
          if-no-files-found: error

  # Build and package all the platform-agnostic(ish) things
  build-global-artifacts:
    needs:
      - plan
      - build-local-artifacts
    runs-on: "ubuntu-22.04"
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.architecture == 'all' }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/global-dist-manifest.json
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false
          submodules: recursive
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.source_ref || github.ref }}
      - name: Install cached dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Get all the local artifacts for the global tasks to use (for e.g. checksums)
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: cargo-dist
        shell: bash
        env:
          TAG_FLAG: ${{ needs.plan.outputs.tag-flag }}
        run: |
          dist_args=(--output-format=json --artifacts=global)
          if [[ -n "$TAG_FLAG" ]]; then
            dist_args+=("$TAG_FLAG")
          fi
          dist build "${dist_args[@]}" > dist-manifest.json
          echo "dist ran successfully"

          # Parse out what we just built and upload it to scratch storage
          {
            echo "paths<<EOF"
            jq --raw-output ".upload_files[]" dist-manifest.json
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-build-global
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}

  validate-posix-installers:
    name: Homebrew and shell installers (${{ matrix.platform }})
    needs:
      - build-global-artifacts
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.architecture == 'all' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux-x86_64
            runner: ubuntu-24.04
          - platform: macos-aarch64
            runner: macos-15
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Download generated installers
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: artifacts
          merge-multiple: true
      - name: Validate shell installers and Homebrew formulae
        shell: bash
        run: |
          set -euo pipefail
          shell_count=$(find artifacts -type f -name '*installer.sh' | wc -l | tr -d ' ')
          formula_count=$(find artifacts -type f -name '*.rb' | wc -l | tr -d ' ')
          test "$shell_count" -gt 0
          test "$formula_count" -gt 0
          find artifacts -type f -name '*installer.sh' -exec bash -n {} +
          find artifacts -type f -name '*.rb' -exec ruby -c {} \;
          if [[ "${{ runner.os }}" == macOS ]]; then
            brew --version
            # cargo-dist owns the generated template's layout. Exclude its fixed
            # structural cops while checking every other formula style rule.
            style_exceptions='FormulaAudit/Homepage,FormulaAudit/Desc,FormulaAuditStrict,Sorbet/StrictSigil,Sorbet/TrueSigil,Style/Documentation,Style/FrozenStringLiteralComment,Style/SoleNestedConditional,Style/MutableConstant,Layout/HashAlignment,Style/TrailingCommaInHashLiteral,Style/IfUnlessModifier'
            tap_name='anytype/ci'
            brew tap-new --no-git "$tap_name"
            tap_root=$(brew --repository "$tap_name")
            while IFS= read -r -d '' formula; do
              cp "$formula" "$tap_root/Formula/$(basename "$formula")"
            done < <(find artifacts -type f -name '*.rb' -print0)
            formulae=("$tap_root"/Formula/*.rb)
            brew style --formula --except-cops "$style_exceptions" "${formulae[@]}"
          fi

  validate-powershell-installer:
    name: PowerShell installer (windows-x86_64)
    needs:
      - build-global-artifacts
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.architecture == 'all' }}
    runs-on: windows-2025
    steps:
      - name: Download generated installers
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: artifacts
          merge-multiple: true
      - name: Parse PowerShell installers
        shell: pwsh
        run: |
          $installers = @(Get-ChildItem artifacts -Recurse -File -Filter '*installer.ps1')
          if ($installers.Count -eq 0) { throw 'no PowerShell installer was generated' }
          foreach ($installer in $installers) {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              $installer.FullName,
              [ref]$tokens,
              [ref]$errors
            ) | Out-Null
            if ($errors.Count -ne 0) { throw ($errors | Out-String) }
          }
