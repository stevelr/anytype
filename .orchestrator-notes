# Orchestrator Notes

- Scope: br ready, types bug/chore/task, priorities 0-2 (no label filter this session).
- Carried in_progress from prior mcp scope: `any-260` (stable MCP protocol default + gated 2026 preview, P1) and `any-nus` (schema token budget + cross-platform CI, P2); triage these before ready-only work.
- Ready queue is the anyr 0.5 CLI gap plan (docs/anyr-0.5-cli-gap-analysis.md) plus `any-q1n` (test-coverage review). Phase ordering from the plan's rollout section is now encoded as br deps: phase-1 (any-ori, any-2b1, any-f5t) -> phase-2 (any-ho5, any-6r7) -> phase-3 (any-vtr, any-ga2) -> phase-4 (any-hcq) -> phase-5 (any-5gx). `any-8tf` (cleanup) and `any-q1n` are unordered.
- Prior mcp-phase history (18 Phase 1 tickets integrated and closed) pruned 2026-07-21; durable code invariants remain in .agent-notes.
