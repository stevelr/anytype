# Orchestrator Notes

- Filter: `mcp`, actionable types bug/chore/task, priorities 0-2.
- Closed this session: `any-cut`, `any-g8g`, `any-kus`, `any-mtc`, `any-ch6`, `any-nzd`, `any-ce6`, `any-8dy`, `any-inq`, `any-ega`, `any-qwo`, `any-tb2`, `any-2fu`, `any-8jk`, `any-gxm`, and `any-rqs` after local and independent acceptance review.
- Audited: all 18 original Phase 1 tickets (`any-kus` through `any-zk6`) plus their concrete anytype-api/any-mcp seams. The deeper seam pass found and corrected missing resolver and response-bound dependencies before handler work.
- Integrated: reviewed `any-kus` and `any-mtc` stacks in `vsonxszx`; combined 53 tests, clippy, format, rustdoc, dependency tree, auth pings, and protocol initialize/EOF smoke passed.
- Integrated: reviewed pagination/validation and resolver stacks in `lrvkyvmz`; combined any-mcp 69 tests, anytype 110/1 ignored, clippy, format, rustdoc, downstream checks, dependency tree, live auth pings, and protocol initialize/EOF smoke passed.
- Dependency corrections: `any-nzd` now blocks `any-inq`, `any-ega`, `any-qwo`, and `any-tb2`, because each accepts resolver-backed space/type/view references. All later mutation/resource consumers are covered transitively; the graph is cycle-clean.
- Added `any-ce6` (P1 security) to bound generic Anytype JSON response reads before handler-side projection; it blocks all four direct Phase 1 handler roots.
- Added `any-8dy` (P1 foundation) for common handler encoding, checked continuation, object-summary conversion, and projected-property schemas; it blocks those same handler roots and prevents parallel implementations from drifting.
- Integrated: reviewed bounded-response and shared-handler stacks in `quoysqrx` (`30363198`); combined any-mcp 89 tests, full anytype integration suite and 111 doctests, clippy, format, rustdoc, downstream checks, dependency tree, live dual-protocol auth, and MCP 2026-07-28 initialize smoke passed.
- Added P3 security OFI `any-zd7` for bounded chat SSE frames and redacted stream URLs. OFIs retained P3: `any-ipy` (safe nested gRPC classification) and `any-8nl` (broader anytype-api diagnostic redaction); none blocks REST-backed Phase 1.
- Integrated: independently reviewed discovery, object-read, view-read, and archive stacks in `swroolpx` (`57968b67`); combined any-mcp 135 tests, full anytype integration suite and 111 doctests, clippy, format, rustdoc, downstream checks, dependency tree, live dual-protocol auth, and MCP 2026-07-28 initialize smoke passed.
- Review fixes included strict omission-versus-null inputs, resolver-output revalidation, view-path hardening in both MCP and anytype-api, tag-count pagination consistency, exact two-call continuation fixtures, and duplicate-query rejection in the fixture parser.
- The create/update R1 reviews exposed two missing shared dependencies. Added and closed `any-8jk` for one canonical mutation-value/schema/semantic-comparison module and `any-gxm` for bounded semantic verification plus stage-aware indeterminate outcome mapping; both explicitly blocked `any-89d` and `any-mll`, and the graph stayed cycle-clean.
- Integrated: reviewed resource and mutation-operational stacks in `mkppnoqu` (`8a0beff7`) on the reviewed mutation-value parent; combined any-mcp 167 tests, full anytype integration suite and 111 doctests, clippy, format, rustdoc, downstream checks, dependency tree, live dual-protocol auth, and MCP 2026-07-28 initialize smoke passed.
- The create/update R2 review found a third shared blocker, `any-rqs`: anytype-api retried mutation 429s and reqwest could replay POST/PATCH below the explicit loop through redirects or caller retry policies. The independently reviewed fix at `mpnqlwno` (`cc22c57a`) makes the method-aware loop the sole retry authority and adds one shared conservative post-dispatch rejection classifier.
- Integrated `any-rqs`: any-mcp 171 tests, full sourced anytype unit/integration suite and 111 doctests, format, clippy, warning-denied rustdoc, dependency tree, diff check, and live HTTP+gRPC auth all passed. P3 OFI `any-az4` records making direct live mutation tests independent of server rate-limit configuration.
- Exact priority 0-2 ready reconciliation is empty because the only unblocked work is already `in_progress`: `any-89d` and `any-mll`. P3 OFIs remain session-deferred.
- Blockers: no external blocker; both active mutation handlers now need their existing R2 work rebased onto `mpnqlwno` and adapted to the shared classifier/exact-once contract. `any-6nj` remains correctly blocked by `any-mll`, and production routing/tests/docs remain downstream.
- Next: resume/rebase create and update R2 stacks one at a time, consume the shared classifier, add exact 429/408 handler recovery fixtures, independently re-review, integrate, close, and re-query.
