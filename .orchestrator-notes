# Orchestrator Notes

- Resolved pre-close: `any-cut` and `any-g8g`; both remain tracker-in-progress for the designated closer.
- Blocked: 18 scoped MCP issues remain dependency-blocked pending closure and subsequent dependency progression.
- Next: close both resolved items, then rerun `br ready -l mcp -p 0 -p 1 -p 2 -t bug -t chore -t task --json` before dispatch.
