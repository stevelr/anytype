# Orchestrator Notes

- Current state
  - Scope: label `mcp`; actionable bug/chore/task priorities 0-2; preserve protected `anyr-0.5` exactly and never rebase/forget/delete it.
  - Phase 1 is complete. `any-lzn`, every `any-cwi` child/parent, and terminal verifier `any-zk6` are closed; final live gates were direct 13/13 and spawned 8/8.
  - Phase 2 dependency audit is complete. Common/schema R8 `any-x90` is independently approved/integrated through `9135237d`; the sole shared contract is `any-mcp/designs/optional-toolsets-common.md`. Schema and members remain; views-write is deferred on `any-b7a3`; admin is omitted. Optional startup must reject retry limits outside 1..=5 and depends on a cumulative six-physical-attempt `anytype-api` cap.
  - Files design `any-hru` R6 is independently approved/integrated through `57d7b2fb`: three/two tools, no resource/template, ordinary Anytype permalink rationale, full-GET SHA-256 verification, and complete resolver/physical/byte accounting.
  - Chats design `any-ii5` R7 is independently approved/integrated through `84bc49fc`: six/four REST-first tools, modified-at delete precondition, exact-ID no-second-POST replay, smaller justified pages, and complete resolver/physical accounting.
  - Disposable isolation design `any-sxbs` R6 is independently approved/integrated through `0620a0d1`. `ANYTYPE_TEST_SPACE_PREFIX` is explicit case-insensitive deletion authority; missing/invalid skips. There is no provenance inventory or 1,000-space cap. Offset-safe sweeps, direct cache-disabled delete authorization, backend lease, durable recovery ledger, and total cleanup precedence are required.
  - Disposable implementation R1 (`b98a99df`) was rejected. R2 (`d162d3e8`) passed fast gates but predates approved design R6 and is also not accepted/integrated. Preserve `mcp-oex` for an R3 only after revised `any-zhpr` approval.
  - Live headless use is serialized. The last disposable sentinel released the reservation; both created IDs were independently absent. Do not rerun until the approved design is implemented.

- Blockers
  - Human gates after designs freeze: `any-hjn` common/schema, `any-ztf` members, `any-kqb` views-write, `any-thu` admin, `any-ldk` files, `any-dml` chats, and `any-zhpr` disposable isolation.
  - Phase 2 implementations remain dependency-blocked until their matching human review closes. Files/chats also depend on corrected disposable isolation (`any-oex`).
  - P3 OFIs retained: `any-3rt`, `any-h94e`, `any-b7a3`, `any-93eo`, `any-e7ei`, bounded property/tag cache work `any-k3q2`, and captured-server-log gap `any-slrb`.

- Next action
  - Present all seven human review gates together with exact tracked documents and retained/deferred surfaces. The filtered actionable queue is empty; do not start dependent implementation before explicit approval.
  - After approval, close the corresponding review gates, implement disposable R3 against design R6, then reconcile `any-1bpb`/`any-rawn`/`any-ngn5`/`any-oex`.
  - Decompose approved schema/files/chats umbrellas and continue Phase 2 implementation via isolated subagent workspaces.
