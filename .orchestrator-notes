# Orchestrator Notes

- Current state
  - Scope: label `mcp`; actionable bug/chore/task priorities 0-2. Never edit/remove the protected `anyr-0.5` workspace or perform anyr changes.
  - Phase 1 and its terminal verification are complete. Continue v1/Phase 2 only through encoded dependencies and human review gates.
  - Streamable HTTP design is integrated at `muynsuqu`; security review `any-01c` remains human-blocked before implementation `any-ucd`.
  - Native files common R10/files R8 are integrated at `ttvkxnvr`; independent R2 review is clean. Human review `any-2f0g.31` blocks native file implementation.
  - `any-2f0g.2` bounded anytype-api file evidence is closed/integrated. Combined anytype/any-mcp classifiers and checks pass.
  - Shared filter implementation is integrated at `wxsvyttu`; the implementation-reflecting R1 contract and follow-up code passed independent review with no findings. `any-2f0g.4.1` is closed and human gate `.4.2` is ready; do not close `.4.3`-`.4.5` before approval.
  - Disposable helper R4 was rejected; R5 partial `wrkmputs`/`1b5ca56c` fixes atomic child ownership and several bounded-recovery issues but is blocked on descriptor-safe coherent WAL snapshots. `any-oex.1` designs the amendment and human gate `.2` blocks `any-1bpb`; do not integrate or run the sentinel.
  - Headless-supervisor ADR reached R4 after three independent review rounds. The last delta adds a separately locked bounded multi-candidate log registry and closes probe taxonomy gaps; final focused review is active and human gate `.26` stays blocked.
  - `any-2f0g.18` was reopened because its design is not present in default. New human API-model gate `.32` blocks `.19` and `.22`; `.22` no longer waits for full `.20` implementation. Never inspect, edit, or remove protected `anyr-0.5`.
  - New post-implementation human gates `.33` (rich blocks) and `.34` (headless supervisor) replace direct `.24`/`.29` edges into terminal acceptance `.30`.
  - `any-slrb` is closed: the current terminal-only server has no recoverable history; `any-mcp/TESTING.md` documents private raw capture on the next normal launch and bounded reviewed redacted evidence.

- Blockers
  - Human reviews: `any-01c` HTTP security, `any-2f0g.31` common R10/files R8, and `any-2f0g.4.2` filter DTO design.
  - Disposable snapshot design must explicitly choose a held-descriptor SQLite VFS or a reviewed narrower filesystem trust boundary. After approval, implementation still must finish panic-total evidence, globally bounded sweeps, descriptor-safe cleanup, and the complete adversarial matrix.
  - Anytype test prefix is absent; destructive live disposable tests must skip and must not be run without operator authority.

- Next action
  - Present `.4.2` for human approval; do not retroactively close gated implementation children first.
  - Review disposable R4 independently, then run `any-rawn` only with an approved prefix and proceed to `any-ngn5`.
  - Review the headless-supervisor ADR and body-block design ownership; continue ready non-anyr mcp work through subagents.
  - Reconcile ready/open/in_progress plus parent issues. In addition to the mandated loop-stop query, explicitly run `br ready -l mcp -t ofi --json` so later `any-b7a3` is not invisible.
  - Run combined gates, sync with `br sync --flush-only`, and retain human gates as explicit blockers.
